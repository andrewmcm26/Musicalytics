@model IEnumerable<AudioEmotion>

<h2>Joyful Songs</h2>

<table class="table">
    <tr>
        <th>Name</th>
        <th>Rating</th>
    </tr>

    @foreach (var song in Model)
    {
        <tr>
            <td>@song.songName</td>
            <td>@song.emotionValue</td>
        </tr>

    }
</table>
